echo "make and install"
echo "Usage: %0          ... build on the currently assigned Release level"
echo "Usage: %0 [0|1|2]  ... build on Release level 0--2"

if [ -n "$1" ]; then
  ./__set_app_rel_lvl.sh -
  mk_backup_file appdefs.h
  ./__set_app_rel_lvl.sh "$1"
fi

./__m

strip be
ls -l be
echo -n "To install this, hit Enter: "; read key
sd sudo whoami
sudo sh ./__m install
sh _cp_be_scripts
sh _cp_berc
./_xi18n

if [ -n "@1" ]; then
  cp -v appdefs.h~ appdefs.h
  ./__set_app_rel_lvl.sh -
fi
